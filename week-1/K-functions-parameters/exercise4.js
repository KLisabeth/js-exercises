// Declare your function first
function addNumbers(passing, parameters) {
    return passing + parameters;
    }
// Call the function and assign to a variable `sum`
var sum = addNumbers(13, 124)

console.log(sum);
